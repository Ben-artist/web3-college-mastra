name = "web3-college-mastra-worker"
main = "src/worker.ts"
compatibility_date = "2024-10-25"
compatibility_flags = ["nodejs_compat"]

# 生产环境配置
[env.production]
name = "web3-college-mastra-worker"

# 预发布环境配置
[env.staging]
name = "web3-college-mastra-worker-staging"

# 注意：环境变量（Secrets）需要通过 wrangler secret put 命令设置
# 不会在这里配置敏感信息

# 如果需要使用 KV 存储（可选）
# [[kv_namespaces]]
# binding = "MODERATION_CACHE"
# id = "your-kv-namespace-id"
# preview_id = "your-preview-kv-namespace-id"

